### Compile shared library and kernel
Make sure you are in the correct directory (cutlass/examples/00_basic_gemm).
Run `sh buildall.sh`.

### Pycuda test
The temporary python test file I wrote is `test.py`.